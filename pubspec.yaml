name: combitone_windows
description: Combitone VPN Windows client
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  http: ^1.2.1
  shared_preferences: ^2.3.2
  path_provider: ^2.1.4
  tray_manager: ^0.2.3
  window_manager: ^0.3.9

flutter:
  uses-material-design: true
